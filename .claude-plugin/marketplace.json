{
  "$schema": "https://json.schemastore.org/claude-code-marketplace.json",
  "name": "todokeeper",
  "metadata": {
    "description": "Keeps a repo's deferred-work file honest: measures it, finds entries whose referents moved on without them, finds referents that no longer exist, and splits or archives only when a measured threshold is crossed. Adapts to the structure the repo already uses; reports evidence rather than ruling.",
    "version": "0.1.0"
  },
  "owner": {
    "name": "androsland",
    "email": "4264136+androsland@users.noreply.github.com",
    "url": "https://github.com/androsland"
  },
  "plugins": [
    {
      "name": "todokeeper",
      "source": "./",
      "description": "Keeps a repo's deferred-work file honest: measures it, finds entries whose referents moved on without them, finds referents that no longer exist, and splits or archives only when a measured threshold is crossed. Adapts to the structure the repo already uses; reports evidence rather than ruling."
    }
  ]
}
